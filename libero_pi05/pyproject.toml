[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "legislative_harness"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["legislative_harness*"]
