_target_: planning.gd.GDPlanner
horizon: 5
action_noise: 0.003
sample_type: 'randn' # 'zero' or 'randn'
lr: 1 
opt_steps: 1000
eval_every: 10
env_eval: false  # HARNESS EDIT: off = planning never touches the sim (WM-only); true re-enables in-opt diagnostics

name: gd